export interface Customer {
    id: number;
    name: string;
    surname: string;
    homeAddress: string;
    email: string;
    homeNumber: string;
    workNumber: string;
    cellphoneNumber: string;
}